interface:
  display_name: "Intel Mastery"
  short_description: "Use GoodVibes code intelligence effectively"
  default_prompt: "Use $intel-mastery to choose efficient GoodVibes intel tools for this codebase task."

dependencies:
  tools:
    - type: "mcp"
      value: "goodvibes_intel"
      description: "GoodVibes code intelligence MCP server"

policy:
  allow_implicit_invocation: true
