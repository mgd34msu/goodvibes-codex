interface:
  display_name: "GoodVibes Analytics"
  short_description: "Inspect Codex usage and activity reports"
  default_prompt: "Use $goodvibes-analytics to summarize the available GoodVibes usage and activity data."

dependencies:
  tools:
    - type: "mcp"
      value: "goodvibes_analytics"
      description: "GoodVibes Codex analytics MCP server"

policy:
  allow_implicit_invocation: false
