interface:
  display_name: "GoodVibes Maintenance"
  short_description: "Diagnose and self-heal the GoodVibes plugin"
  default_prompt: "Use $goodvibes-maintenance to inspect GoodVibes health, automatically repair its runtime dependencies, and verify all three MCP servers."

policy:
  allow_implicit_invocation: true
